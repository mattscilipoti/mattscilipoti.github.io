.bundle/
.sass-cache/

# Gemfile
vendor/bundle
vendor/cache

# Jekyll
_site/
.jekyll-cache/
.jekyll-metadata
# For Jekyll, ignore Gemfile.lock because Github pages uses it's own pre-defined environment
Gemfile.lock
# For Jekyll, ignore vendor directory because it is unused by Jekyll, is large, and can be generated by running bundle install
vendor/